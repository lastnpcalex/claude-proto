Fetch a Bluesky post by its AT-URI (no login required).

Usage: /bsky-get-post <at-uri>

Run this exact command, replacing AT_URI from $ARGUMENTS:

    python "C:/Users/exast/.claude/commands/bsky_cli.py" post AT_URI

If $ARGUMENTS contains --verbose, display the full command output to the user exactly as returned — do not summarize or omit anything. Otherwise display a summary.