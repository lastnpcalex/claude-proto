Resolve a Bluesky handle to its DID (no login required).

Usage: /bsky-resolve <handle>

Run this exact command, replacing HANDLE from $ARGUMENTS:

    python "C:/Users/exast/.claude/commands/bsky_cli.py" resolve HANDLE

Display the output to the user. The DID can be used as the actor parameter in other /bsky-* commands.